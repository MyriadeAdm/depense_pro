import 'package:flutter/material.dart';

class DetailsDepense extends StatelessWidget {
  const DetailsDepense({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Details Dépenses"),
      ),
      body: Center(
        child: Column(
          children: [
            
          ],
        ),
      ),
    );
  }
}
